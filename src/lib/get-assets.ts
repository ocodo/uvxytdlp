/* import { useApiBase } from "@/contexts/api-base-context"
import { createApiFetch } from "@/lib/api-fetch"

const  apiFetch = createApiFetch()

export const getAssets = async (filename: string): string[] => {

  const response = await apiFetch()
}
  */